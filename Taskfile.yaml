# Docs: https://taskfile.dev/#/usage
version: "3"

tasks:
  format:
    cmds:
      - go tool gofumpt -l -w .

  lint:
    cmds:
      - go tool golangci-lint run .

  test:
    cmds:
      - go test ./...

  build:
    cmds:
      - go build -v -o ./bin/ .

  run:
    cmds:
      - go run .
